#_________________________________
# round off the given value in the for the selected decimal
x = round(5.76543, 2)
print(x)
op:-5.77
#_____________________________________________________
#The Python abs() function return the absolute value. The absolute value of any number is always positive it removes the negative sign of a number in Python. 
  diff_cost = abs(cost_l -cost_d) 
#________________________________________________________________-
#if we cant to calculate the percentage then we have to use this in python 
#value that represents the similarity or dissimilarity between two values
#which can be useful in various situations where you want to quantify how much one value deviates from another relative to a reference value.
thirty_cost = 30 
stand_cost = 8
res = 1 - abs(thirty_cost - stand_cost) / stand_cost
#_________________________________________________________________________

_#  user_int_sess_ids = set(user_session_ids.keys()).intersection(set(user_session_id_dict.keys()))
    host_int_sess_ids = set(host_session_ids.keys()).intersection(set(host_session_id_dict.keys()))
       
_______________________________________________________________________________________________-

